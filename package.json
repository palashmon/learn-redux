{
  "name": "learn-redux",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "browser-sync": "^3.0.2"
  },
  "scripts": {
    "start":
      "browser-sync start --server --files '**/*.css, **/*.html, **/*.js, **/*.jsx, !node_modules/**/*' --directory --port 7777"
  }
}
